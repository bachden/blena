# Uncomment the next line to define a global platform for your project
platform :ios, '12.0' # Update the iOS version to match your minimum deployment target

# Main target
target 'Blena' do
  # Use dynamic frameworks
  use_frameworks!

  # Pods for Blena
  pod 'EasyTipView'

  # Unit Tests target
  target 'BlenaTests' do
    inherit! :search_paths
    # Add any pods specific to testing
    # e.g., pod 'Quick', pod 'Nimble'
  end

  # UI Tests target
  target 'BlenaUITests' do
    inherit! :search_paths
    # Add any pods specific to UI testing
    # e.g., pod 'EarlGrey'
  end
end

# Share extension target
target 'shareextension' do
  use_frameworks!

  # Pods for shareextension
  # Add any necessary pods here
end

# Widget extension target
target 'widget' do
  use_frameworks!

  # Pods for widget
  # Add any necessary pods here
end